# Write-Behind Caching  Pattern - using Redis & Spin Applications

configure the recipe to redis using gears-cli
``````
```
gears-cli --host <host> --port <port> --password <password> run example.py
```


